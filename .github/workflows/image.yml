name: Build All Images

on: [workflow_dispatch]

jobs:
  build-latest:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push latest
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./8.4/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:latest, toasterlint/php-apache-mysql:8.4-nowarn, toasterlint/php-apache-mysql:8.4, toasterlint/php-apache-mysql:8
  build-7_4:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push 7.4
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./7.4/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:7.4-nowarn, toasterlint/php-apache-mysql:7.4, toasterlint/php-apache-mysql:7
  build-8_0:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push 8.0
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./8.0/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:8.0-nowarn, toasterlint/php-apache-mysql:8.0
  build-8_1:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push 8.1
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./8.1/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:8.1-nowarn, toasterlint/php-apache-mysql:8.1
  build-8_2:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push 8.2
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./8.2/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:8.2-nowarn, toasterlint/php-apache-mysql:8.2
  build-8_3:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push 8.3
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./8.3/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: toasterlint/php-apache-mysql:8.3-nowarn, toasterlint/php-apache-mysql:8.3